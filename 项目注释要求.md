# 团队代码注释规范

## 1. 目的

本规范用于统一团队代码注释的写法，提升以下方面的质量：

- **可读性**：让他人能快速理解代码意图
    
- **可维护性**：降低后续修改出错概率
    
- **可协作性**：让不同成员在同一套工程语义下沟通
    
- **可演进性**：避免注释成为误导信息或历史垃圾
    

本规范适用于：

- 业务代码
    
- 基础库 / 工具库
    
- 中间件接入代码
    
- 脚本、任务、服务端、客户端、数据处理代码
    
- 配置生成逻辑、部署辅助代码、测试中关键逻辑
    

---

## 2. 总体原则

### 2.1 注释的目标不是“翻译代码”，而是补充代码无法自然表达的信息

注释应优先说明：

- **为什么这样实现**
    
- **有哪些约束**
    
- **哪些地方容易误用**
    
- **这段逻辑依赖什么假设**
    
- **这段写法为什么不能轻易改**
    

不应仅仅复述：

- 变量赋值
    
- 条件判断
    
- 循环行为
    
- 代码字面动作
    

---

### 2.2 注释必须服务于未来维护者

默认读者不是当前作者，而是：

- 半年后的你自己
    
- 新加入项目的同事
    
- 不熟悉该模块的人
    
- 在故障、重构、性能优化时接手的人
    

---

### 2.3 注释宁缺毋滥，但关键处不能缺

少写无价值注释，多写高价值注释。

判断一条注释是否值得写，至少看三点：

- 没有这条注释，别人是否容易误解或改错
    
- 代码本身是否已经足够表达这层信息
    
- 这条注释是否相对稳定，不容易快速过时
    

---

### 2.4 注释必须与代码一起维护

修改代码时，若影响注释语义，必须同步修改注释。  
**过时注释比没有注释更糟。**

---

## 3. 注释应该写什么

## 3.1 应写内容

以下信息属于高价值注释内容。

### A. 设计意图

说明为什么采用当前实现，而不是别的实现。

示例：

```text
不直接物理删除记录，而是做软删除。
原因：审计链路依赖该记录持续可追踪。
```

---

### B. 业务约束

说明代码背后的业务规则，而不仅是技术动作。

示例：

```text
订单一旦进入已结算状态，不允许再次修改金额。
这是财务对账约束，不是前端展示限制。
```

---

### C. 输入 / 输出契约

说明：

- 参数范围
    
- 空值约定
    
- 返回结果顺序
    
- 默认值语义
    
- 是否允许部分成功
    

示例：

```text
返回结果与输入 ID 顺序保持一致。
未命中的位置返回空，不抛异常。
```

---

### D. 副作用

说明函数或模块除了“主结果”外还会做什么。

示例：

```text
该操作除写数据库外，还会发送审计事件并刷新本地缓存。
```

---

### E. 异常 / 错误语义

说明何时失败、失败方式、失败后调用方应如何理解。

示例：

```text
当外部依赖超时时返回可重试错误；
当参数非法时返回不可重试错误。
```

---

### F. 边界条件 / 陷阱

说明哪些输入、状态、时序、环境条件下容易出问题。

示例：

```text
这里必须使用整数金额，避免浮点精度误差导致结算失败。
```

---

### G. 性能和并发相关原因

说明特殊写法是不是因为性能、一致性、锁、缓存、幂等等原因。

示例：

```text
这里先聚合后批量查询，是为避免 N+1 查询。
```

```text
调用方必须已持有锁；此方法不重复加锁，避免死锁和额外锁竞争。
```

---

### H. 兼容性 / 历史包袱

说明旧逻辑为何仍然存在，以及未来移除条件。

示例：

```text
兼容旧版客户端的字段名映射。
待旧版本覆盖率低于 5% 后可删除该分支。
```

---

## 3.2 不应写内容

以下内容原则上不应写成注释。

### A. 复述代码字面意思

反例：

```text
给 count 加 1
判断用户是否为空
遍历列表
```

---

### B. 明显可通过命名表达的内容

如果能通过更好的命名解决，就不要靠注释补救。

反例：

```text
x 是订单金额
```

更好的做法是把 `x` 改成 `orderAmountInCents` 之类的清晰命名。

---

### C. 情绪化、口语化、不可验证内容

反例：

```text
这里很坑，不要乱动
这个写法比较神奇
理论上不会有问题
```

这些内容没有工程可验证性，也没有维护指向性。

---

### D. 用注释掩盖糟糕设计

反例：

```text
这个函数比较长，但先不要拆，拆了会出问题
```

这不是有效注释，而是设计债务声明。应考虑重构，或把真正约束说明白。

---

## 4. 注释层级要求

注释必须分层，而不是全部堆在函数内部。

## 4.1 文件 / 模块级注释

### 必须在以下情况添加

- 该文件承担明确职责
    
- 文件内含核心领域模型或重要流程
    
- 文件名无法完整表达边界
    
- 容易与相邻模块职责混淆
    

### 应说明内容

- 本模块负责什么
    
- 不负责什么
    
- 与其他模块的关系
    
- 关键约束或设计原则
    

### 推荐模板

```text
模块职责：
- ...

不负责：
- ...

关键约束：
- ...

依赖 / 协作关系：
- ...
```

---

## 4.2 类型 / 类 / 结构体 / 组件级注释

### 适用对象

- 类
    
- 接口
    
- 结构体
    
- 组件
    
- 领域对象
    
- 数据载体
    
- 服务对象
    
- 适配器
    
- 策略实现
    

### 应说明内容

- 该抽象表示什么
    
- 它在系统中的角色是什么
    
- 它与相似抽象的区别是什么
    
- 生命周期 / 可变性 / 线程安全性等特征
    

### 推荐模板

```text
表示：
- ...

用途：
- ...

注意：
- ...
```

---

## 4.3 函数 / 方法级注释

### 必须注释的函数

- 对外暴露的公共接口
    
- 核心业务函数
    
- 复杂流程函数
    
- 有明显副作用的函数
    
- 约束较多、容易误用的函数
    
- 名称无法完全表达契约的函数
    

### 可省略注释的函数

- 非公共、逻辑极简单、名称与行为高度一致的小函数
    
- 仅作单一转发且无额外语义的小包装函数
    

### 应说明内容

- 功能
    
- 参数约束
    
- 返回值语义
    
- 副作用
    
- 错误 / 异常
    
- 调用前提
    
- 性能 / 一致性 / 顺序要求（如有）
    

### 推荐模板

```text
功能：
- ...

参数：
- ...

返回：
- ...

前置条件：
- ...

副作用：
- ...

错误 / 异常：
- ...

说明：
- ...
```

---

## 4.4 代码块 / 行内注释

### 只在以下情况使用

- 局部逻辑不直观
    
- 有特殊顺序要求
    
- 有算法 / 性能原因
    
- 有临时兼容逻辑
    
- 有易被误删 / 误改的防御性写法
    

### 写法要求

- 只解释局部难点
    
- 应尽量简短
    
- 应贴近所解释代码
    
- 不要连续用大量行内注释把代码写成“散文”
    

### 推荐模板

```text
为什么这样做：
- ...

注意：
- ...
```

---

## 5. 特定场景强制要求

以下场景必须写注释，不能省略。

## 5.1 魔法数字 / 特殊常量

如果某个数字、阈值、枚举映射不是自解释的，必须说明来源或意义。

反例：

```text
重试 7 次
```

正例：

```text
最多重试 7 次，对应上游网关 30 秒超时窗口内的最大安全重试次数。
```

---

## 5.2 特殊排序 / 去重 / 聚合规则

若顺序、优先级、覆盖关系影响业务结果，必须说明。

正例：

```text
按“人工配置 > 租户默认 > 系统默认”的优先级合并。
后者只补前者缺失项，不覆盖已有值。
```

---

## 5.3 并发、锁、事务、一致性

必须说明：

- 是否要求外层已持锁
    
- 为什么在这里开启 / 不开启事务
    
- 为什么允许最终一致
    
- 幂等边界在哪里
    

---

## 5.4 缓存与失效策略

必须说明：

- 缓存键语义
    
- 命中 / 回源逻辑
    
- 失效条件
    
- 是否允许脏读或短暂不一致
    

---

## 5.5 外部系统协议 / 第三方限制

必须说明：

- 外部字段语义
    
- 时间单位、金额单位
    
- 大小写、编码、签名、时区约定
    
- 接口的异常约束或兼容历史
    

---

## 5.6 临时兼容逻辑 / workaround

必须包含：

- 为什么存在
    
- 影响范围
    
- 预期移除条件
    

推荐格式：

```text
临时兼容原因：
- ...

影响范围：
- ...

移除条件：
- ...
```

---

## 6. 注释风格要求

## 6.1 语言风格

- 使用陈述句
    
- 避免口语化和情绪化表达
    
- 避免模糊词：`可能`、`大概`、`理论上`
    
- 尽量使用明确术语：如“幂等”“软删除”“最终一致”“只读快照”
    

---

## 6.2 长度控制

- 行内注释：应短
    
- 函数级注释：完整但不冗长
    
- 模块级注释：聚焦职责与边界，不写实现流水账
    

原则：**信息密度高于字数多少。**

---

## 6.3 稳定性优先于实现细节

优先描述：

- 设计意图
    
- 契约
    
- 约束
    
- 风险
    

尽量少描述会频繁变化的实现步骤。

反例：

```text
先查缓存，再查数据库，再拼装对象
```

更好的写法：

```text
优先使用低成本数据源，未命中时再回源完整数据，以控制读放大。
```

---

## 6.4 与命名配合，而不是替代命名

若必须靠注释解释变量名、函数名、类名，则应优先考虑改名。

---

## 7. 文档注释建议格式

不同语言的注释语法不同，但内容结构尽量统一。  
无论使用 `/** */`、`///`、`#` 还是文档字符串，都建议统一信息组织方式。

## 7.1 公共接口推荐字段

```text
功能：
参数：
返回：
前置条件：
副作用：
错误 / 异常：
说明：
```

不是所有字段都必须出现，但公共接口至少应覆盖：

- 功能
    
- 参数
    
- 返回
    
- 错误 / 异常
    

---

## 7.2 模块推荐字段

```text
模块职责：
不负责：
关键约束：
依赖关系：
```

---

## 7.3 临时事项推荐字段

```text
TODO(负责人/任务号)：
- 计划做什么
- 触发条件或时机

FIXME：
- 当前问题是什么
- 风险是什么
```

---

## 8. TODO / FIXME / NOTE 使用规范

## 8.1 TODO

表示明确的后续工作，不是随手占位。

### 必须包含

- 负责人、任务号或明确归属
    
- 未来动作
    
- 触发条件或时间点
    

正例：

```text
TODO(team-search, ISSUE-184):
当索引切分完成后，将这里替换为批量接口，避免单条回源。
```

反例：

```text
TODO: 以后优化
```

---

## 8.2 FIXME

表示已知问题，且当前实现有风险或缺陷。

### 必须包含

- 问题本身
    
- 影响范围
    
- 最好补充风险
    

正例：

```text
FIXME:
取消请求时这里仍可能提交下游任务，导致重复处理。
在引入请求级取消信号后修复。
```

---

## 8.3 NOTE

表示重要但非缺陷的信息，用于强调约束或背景。

正例：

```text
NOTE:
该字段来自外部账务系统，语义与内部“支付时间”不同，不能混用。
```

---

## 9. 示例

## 9.1 差的注释

```text
遍历订单列表
如果状态是有效
就进行处理
```

问题：

- 纯复述代码
    
- 没有增加任何语义信息
    
- 维护者看了仍然不知道为什么这样做
    

---

## 9.2 好的注释

```text
这里只处理“已确认但未归档”的订单。
已取消订单不能进入后续流程；已归档订单重复处理会导致审计重复记账。
```

优点：

- 解释了过滤条件的业务原因
    
- 明确了错误修改的后果
    
- 有维护价值
    

---

## 9.3 函数注释示例

```text
功能：
批量加载用户权限快照。

参数：
- userIds：待查询用户 ID 列表；允许重复输入
- useCache：是否允许使用短期缓存结果

返回：
- 与输入顺序一致的权限快照列表
- 未找到的用户返回空位，不抛异常

副作用：
- 允许命中 60 秒本地缓存

错误 / 异常：
- 上游权限服务不可用时返回可重试错误

说明：
- 返回的是查询时刻快照，不保证后续实时更新
- 保持输入顺序是为了让调用方无需再次做映射
```

---

## 9.4 局部逻辑注释示例

```text
先按租户分组再批量拉取，避免对权限服务产生 N+1 请求。
```

---

## 10. Code Review 检查清单

评审注释时，至少检查以下问题。

## 10.1 是否应该有注释却没有

- 公共接口是否说明契约
    
- 复杂逻辑是否说明原因
    
- 特殊分支是否说明约束
    
- 临时兼容代码是否说明清理条件
    

---

## 10.2 是否写了不该写的注释

- 是否只是复述代码
    
- 是否存在废话型注释
    
- 是否靠注释掩盖糟糕命名
    
- 是否存在情绪化表达
    

---

## 10.3 注释是否准确

- 与当前代码行为是否一致
    
- 与当前业务语义是否一致
    
- 是否已经过时
    
- 是否存在模糊表述
    

---

## 10.4 注释是否真正有助于维护

- 是否说明了“为什么”
    
- 是否说明了“不能随便改的原因”
    
- 是否说明了“输入输出约束”
    
- 是否让新维护者更快建立上下文
    

---

## 11. 落地执行建议

## 11.1 团队最低要求

建议至少落地以下硬规则：

### 必须

- 公共接口必须有文档注释
    
- 核心模块必须有模块级注释
    
- 复杂逻辑必须写“为什么”
    
- TODO / FIXME 必须带上下文，不允许裸写
    
- 改代码时必须同步改相关注释
    

### 不允许

- 翻译代码式注释
    
- 情绪化注释
    
- 无归属的 TODO
    
- 明显过时但不修的注释
    

---

## 11.2 与静态检查或评审流程配合

可在团队流程中加入：

- 新增公共 API 时检查是否有文档注释
    
- Code Review 模板增加“注释是否准确且必要”
    
- 对 `TODO:`、`FIXME:` 做简单规则检查
    
- 对核心目录启用更严格标准
    

---

## 11.3 与架构文档分工

注释不替代架构文档。

### 注释适合写

- 局部设计意图
    
- 调用契约
    
- 约束
    
- 边界
    
- 易错点
    

### 架构文档适合写

- 系统全局流程
    
- 模块关系图
    
- 跨服务协作
    
- 演进方案
    
- 大规模设计决策
    

原则是：  
**全局问题放文档，局部关键语义放代码注释。**

---

## 12. 最终准则

团队成员在写注释前，应默认回答下面四个问题：

1. 这条信息代码本身是否已经表达清楚？
    
2. 不写这条注释，未来维护者是否容易误解或改错？
    
3. 这条注释写的是稳定的意图和约束，还是容易过时的细节？
    
4. 这条注释是否在降低理解成本，而不是制造噪音？
    

只有当第 2 个问题答案明确是“会”，且其余问题也基本成立时，这条注释才值得写。

---

# 一页版执行摘要

可以把下面这段作为团队最简规范贴在仓库首页：

## 代码注释四原则

- **不翻译代码，只补充代码表达不了的信息**
    
- **优先写为什么、约束、边界、副作用**
    
- **公共接口、复杂逻辑、兼容分支必须注释**
    
- **注释与代码一起维护，过时注释必须删除或更新**
    

## 必须注释的内容

- 公共接口的输入输出契约
    
- 复杂逻辑的设计原因
    
- 并发 / 事务 / 缓存 / 幂等约束
    
- 兼容逻辑和 workaround 的存在原因与移除条件
    

## 禁止的注释

- 复述代码
    
- 情绪化表达
    
- 裸 TODO
    
- 已失效但未更新的注释
    

---
